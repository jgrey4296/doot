# -*- mode:toml; -*-
doot_version = "1.1.1"

# reminders:
# action = [{do= "basic", args=[]}]
# cli    = [{name="-atoggle"}, {name="-akey", type="str"}, {name="--assign=", type="int"}]

[[global]] # global state updates

[[locations]] # Location keys
src     = "src"
codegen = "_codegen"
temp    = ".temp"
build   = "build"
docs    = "docs"
data    = "data"

[tasks]

[[tasks.group]]
name        = ""
print_level = "INFO"
ctor        = "task"
doc         = []
depends-on  = []
actions     = []
